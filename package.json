{
  "name": "inv-utils",
  "version": "2.0.2",
  "description": "",
  "main": "build/inv-utils.js",
  "scripts": {
    "test": "mocha --compilers coffee:coffee-script/register",
    "test-watch": "mocha --compilers coffee:coffee-script/register --watch",
    "build": " coffee -o build -c src ",
    "build-watch": "rm -rf build; coffee --watch -o build -c src",
    "dist": "browserify build/inv-utils.js -s invUtils > dist/inv-utils.js",
    "minimize": "uglifyjs dist/inv-utils.js -c -m > dist/inv-utils.min.js",
    "prepublish": "npm run test && npm run build && npm run dist"
  },
  "keywords": [
    "utils"
  ],
  "author": "maxlath",
  "license": "MIT",
  "devDependencies": {
    "bluebird": "^3.4.1",
    "inv-loggers": "^3.2.5",
    "mocha": "^2.5.3",
    "should": "^9.0.2",
    "underscore": "^1.8.3"
  }
}
